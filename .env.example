# Use a URL com "Connection pooling" ativado no painel do Neon.
DATABASE_URL=postgresql://usuario:senha@ep-seu-projeto-pooler.neon.tech/neondb?sslmode=require

# Ativa a API durante `netlify dev`. Builds publicados usam a API automaticamente.
VITE_USE_NEON=true
